# To find the version of Boost.Test in the image

The run the following:

docker run --rm -it "$(jq -r .image_name manifest.json)" \
  bash -c "dpkg -s libboost-dev | grep 'Version'"

$ Version: 1.74.0.3
